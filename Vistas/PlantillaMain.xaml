<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--Plantilla para botones de AltaAlumno-->
    <ControlTemplate x:Key="AlumnoButtonTemplate" TargetType="Button">
        <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    CornerRadius="10"
                    BorderBrush="Black"
                    BorderThickness="2">
            <ContentPresenter x:Name="contentPresenter" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center" 
                              RecognizesAccessKey="True" 
                              TextElement.FontSize="{TemplateBinding FontSize}" 
                              TextElement.FontWeight="{TemplateBinding FontWeight}" 
                              TextElement.Foreground="{TemplateBinding Foreground}"/>
        </Border>
        <ControlTemplate.Triggers>

            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
            </Trigger>

            <Trigger Property="IsPressed" Value="True">
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                    </Setter.Value>
                </Setter>
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="border" Property="Background" Value="#9E9E9E"/>
                <Setter Property="Opacity" Value="0.7"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="btn_plantilla_registrar" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource ResourceKey=AlumnoButtonTemplate}"></Setter>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#1976D2"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <Style x:Key="btn_plantilla_limpiar" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource ResourceKey=AlumnoButtonTemplate}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#455A64"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <Style x:Key="btn_plantilla_atras" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource ResourceKey=AlumnoButtonTemplate}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#B71C1C"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <Style x:Key="btn_plantilla_modificar" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource ResourceKey=AlumnoButtonTemplate}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#C9A01E"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <Style x:Key="btn_plantilla_eliminar" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource ResourceKey=AlumnoButtonTemplate}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#D14F7D"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>
    <!--Plantilla para botones de recorrido de listas-->
    <Style x:Key="btn_plantilla_lista" TargetType="Button">
        <Setter Property="Template" Value="{StaticResource ResourceKey=AlumnoButtonTemplate}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Background" Value="#141313"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>
</ResourceDictionary>